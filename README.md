# GoBashParser

This is a "bash parser" program, written in Golang.
It supports the following bash commands:

 * `ls` Command
 * `cd` Command
 * `rm` Command
 * `mv` Command
 * `cp` Command
 * `mkdir` Command
 * `pwd` Command
 * `touch` Command
 * `cat` Command
 * `less` Command
